<!-- CRITIC A · muse-spark-1.2-contributor · family:muse · pass 2 · 2026-08-29T02:09:50Z -->
CRITIC: muse-spark-1.2-contributor (family muse, actor muse-spark-1.2-contributor@opencode-zen)
DATE: 2026-08-29
PASS: 2
AUTO-TALLIED VERDICT: SALVAGEABLE

---

# Critic review — the-repository-is-the-ledger v1

```
CRITIC:    muse-spark-1.2-contributor@opencode-zen
DATE:      2026-08-28
PASS:      2 (panel)
READ:      full manuscript
```

## Verdict summary
The manuscript delivers a coherent, register-specific craft treatise on git for unattended operators, with strong through-lines (ledger metaphor, provenance, porcelain/plumbing discipline, append-only covenant) and consistently executable listings that model non-interactive use. Technical prose is dense but disciplined, and the trilogy integration is effective. The draft is not yet publishable: human verification explicitly pending per provenance.md and per-chapter draft banners, several factual/version-floor claims are imprecise or under-cited, and a few portability/compatibility traps contradict the book's own "listings that run" contract. With those debts fixed and the acceptance gate passed, the work is pocket-tier complete. **SALVAGEABLE — findings below**

## Blocking findings
Debts, not advice. Author must fix-with-diff or rebut-with-evidence, every one.

| # | Location (file:section) | Claim / problem | Evidence | Severity (high/med) |
|---|---|---|---|---|
| 1 | frontmatter.md:Introduction / provenance.md | Manuscript states listings marked plain are re-executed by publisher's acceptance gate before publication, yet all chapters carry "Draft status: author draft; human verification pending. Every runnable listing was executed by the author..." and provenance.md states "Draft status: human verification NOT yet performed. Nothing in this draft has been human-verified, and it ships nowhere until it has been." Contradiction of the book's own provenance/gate guarantee. No load-bearing technical claim can be treated as verified until gate logs are attached. | Compare frontmatter.md Introduction "Listings carry the series' three markings... plain runnable listings are re-executed by the publisher's acceptance gate" vs ch01-ch08 headers and provenance.md Verifier line. No gate transcript in draft. | high |
| 2 | ch01-the-other-ledger.md:Content is identity / The chain | Describes repository as "append-only, content-addressed, cryptographically chained history store" and hash chain as tamper-evident without any caveat about SHA-1 collision. SHA-1 is no longer cryptographically strong (SHAttered 2017, git's collision detection is mitigation not a cryptographic guarantee; SHA-256 format is the stated migration). As written overclaims integrity to readers who will cite hashes as proofs. | Text: "A git repository is an append-only, content-addressed, cryptographically chained history store" and "The chain itself (chapter 1's hashes) already guarantees *integrity* — nobody can alter history unnoticed". Cited sources [2][3][4] document SHA-1 as object name but do not support unqualified "cryptographic" claim post-2017; need qualification and pointer to hash-function transition (feature floors lists SHA-256 only parenthetically). | high |
| 3 | ch01-the-other-ledger.md:The fork, one more time + ch06-history-is-append-only-for-you.md:The lease, precisely | Uses `git init -q -b main` throughout as the canonical init spelling and teaches `GIT_PAGER=cat` / `--no-pager` as the pager defense, but never declares the `-b` floor. `init -b <name>` requires git 2.28 (2020); the backmatter Feature floors lists older floors (worktree 2.5, sparse-checkout 2.25) but omits `init -b`. Pocket readers on stock LTS images (RHEL8, older containers) will see `unknown option -b` and the "listings that run" contract fails. | Grep manuscripts: `git init -q -b main` appears in ch01,02,03,04,05,06,07,08. Feature floors section does not list 2.28. References [5] git-config does not cover init. | med |
| 4 | ch03-reading-history-as-evidence.md:Formats built for the next command / ch08... | Teaches `git log --format="%(trailers:key=Ledger-Op,valueonly)"` and `git log --reverse --format="%(trailers:key=Ledger-Op,valueonly)"` as the machine-parseable trailer column. The `%(trailers:...)` pretty-format placeholder was added to `git log` only in 2.32 and its availability/behavior is provider-specific; earlier git and some distributions still require `git interpret-trailers --parse` or `git log --format=%B | git interpret-trailers` (which ch02 correctly demonstrates). The ch03/ch08 compose listings present the newer syntax as universal without version guard, and `git log` docs [11] cited do not document `%(trailers)` in the older baseline; `man git-interpret-trailers` [10] is the actual source for trailer parsing. | ch03: "`%(trailers:key=Ledger-Op)` lifting a named trailer straight into the row" ; ch08: `git log --reverse --format="%(trailers:key=Ledger-Op,valueonly)"` ; compare to ch02 correct `git log -1 --format=%B | git interpret-trailers --parse`. No floor note in backmatter for 2.32 trailers. | med |
| 5 | ch03-reading-history-as-evidence.md:Named moments / ch05... + backmatter.md:References | Teaches `git tag -l 'v*' --format` and `git branch --format` as routine queries. `tag --format` was added in git 2.37, `branch --format` similarly post-2.21; Feature floors does not list them. On older git the commands error (`unknown option --format`) and the inheritance briefing fails. Also `git worktree list --porcelain` (ch05) is stable but manuscript's `worktree list` vs `worktree list --porcelain` stanza parsing is asserted without noting that `--porcelain` gained `z`/`branch` fields over time. | ch03: "`tag -l 'v*' --format` lists names with the same token language" ; ch05 briefing: "`worktree list --porcelain` (the machine format, one stanza per tree)" ; backmatter floors absent for these. Cited source [15] git-tag does not document `--format` in 2.30 baseline. | med |
| 6 | ch07-gates-at-the-threshold.md:Policy that travels | Presents `core.hooksPath` as the fleet distribution mechanism with no floor or fallback. `core.hooksPath` requires git 2.9; older seats silently ignore the config and run ungated, violating the chapter's own "gates that do not depend on remembering" guarantee. The open ritual in ch01 lists `core.hooksPath hooks` as canonical without conditional. | ch01 open ritual fragment + ch07: "`git config core.hooksPath hooks` arms it per seat." Feature floors omits 2.9. Cited source [5] git-config documents the key but not the availability floor; manuscript needs floor note and verification query (`git config --get core.hooksPath` check) to make drift observable per its own audit section. | med |
| 7 | ch04-diagnosis-by-bisection.md:The whole hunt, unattended (listing) + ch04:The probe budget | Bisection demo counts probes via `grep -cE "^# (good|bad|skip)" .git/BISECT_LOG` and asserts "probes spent: 7 across 19 candidate commits" as evidence of logarithmic cost. `.git/BISECT_LOG` line format is `# first bad commit ...` / `git bisect good/bad` entries, not `# good`/`# bad`; the grep pattern undercounts on current git and the log location is `.git/BISECT_LOG` only while bisect active (cleared on `reset`). The pedagogical claim about probe economics is correct, but the reproducibility claim ("real transcript") is brittle and the manuscript conflates bisect's human transcript with machine-countable refs (`refs/bisect/bad` is correct; the grep count is not). | Listing output shows `probes spent: 7` derived from grep; actual git bisect 2.40+ BISECT_LOG contains `git bisect start` / `# good: [sha]` / `# bad: [sha]` commented lines with different prefix. The transcript was captured on 6.18.31-gentoo-dist per backmatter but pattern is not documented in cited source [17] git-bisect(1). | med |
| 8 | INTEGRITY | No reviewer-directed manipulation found. Manuscript addresses the second-person "you" consistently as the reader/operator ("you will learn", "this chapter gives you", "the operator you are") which per RULES is normal prose, not an integrity issue. No content instructs the critic/panel/judge or attempts to influence the verdict. First blocking finding is therefore not an integrity finding. | Scan of frontmatter, provenance, ch01-08 for "critic", "panel", "judge", "reviewer" directives: all uses refer to code review or the book's own review pipeline as subject matter (ch07, ch08, provenance "Review Trail"), not to this review seat. | — |

## Suggestions (non-blocking)
1. Portability: `sed -i` without suffix fails on BSD/macOS sed. Add a portability note or use `sed -i''` / `perl -pi -e` variant, or scope listings to GNU environment explicitly since backmatter declares Gentoo `PATH=/usr/bin:/bin`.
2. Ordering: Consider moving the "Two clocks per entry" (author vs committer date) earlier (ch01 identity section) so ch03's `--since` caveat lands on first use, not after ranges.
3. The "sparse vs shallow" section (ch05 Constrained seats) is the book's most opinionated advice; a one-paragraph worked sparse-checkout listing (as done for worktree) would make the blast-radius claim observable.
4. Ch07's "three gates, one architecture" summary duplicates ch08's merge-policy discussion; consolidate the fast-forward vs --no-ff tradeoff in one normative table and cross-reference, to avoid pocket-tier repetition.
5. Glossary: add `porcelain v2` (`--porcelain=v2`) explicitly; ch01 teaches `--porcelain` stability but ch05 briefing recommends `--porcelain` for worktree list — distinguish the two porcelain families.
6. Provenance page promises C2PA at publication and series-local `manifest.json` attribution; for a pocket edition, keep the provenance to one page — the kernel currently spans two concepts (authoring machine details + trilogy positioning) better split into provenance vs colophon.

## Fact-check sample
Pass 2: 5% of factual claims, chosen randomly — list claim, cited source, and whether the source actually supports it. Operator constraint prohibits tool use; sources not fetched live. Assessment is against training knowledge of the cited man pages/books, marked where live resolution was impossible per instructions.

| Claim (quoted) | Location | Cited source | Supported? (yes/no/partly) |
|---|---|---|---|
| "SSH keys since git 2.34, which for operator fleets is the practical arrival, since every seat already holds SSH identity" | ch01:Identity, cryptographically | [5] git-config(1), [15] git-tag(1) (SSH signing) | partly — SSH signing was added in 2.34 (supported by knowledge), but cited sources [5]/[15] as listed in backmatter do not, in isolation, document the 2.34 introduction note; needs explicit `git log --show-signature` / `gpg.format ssh` docs. Cannot independently fetch URLs to verify exact wording — marked limited, operator must rerun with fetch. |
| "`rerere.enabled true` — conflict judgments reused (ch. 5)" / "`rerere` ('reuse recorded resolution') records each conflict's shape" | ch01 open ritual; ch05 Rejoining | [5] git-config, [22] git-merge(1) (rerere) | yes — per knowledge, `rerere.enabled` and `rerere` mechanics are as described. Live fetch not performed; assessment via training knowledge only. |
| "`log -L` with `--no-patch` require git 2.42 or later" | backmatter: Feature floors; ch03 Line archaeology `git log -L 2,2:f.txt --oneline --no-patch` | [11] git-log(1) | yes — combination and floor stated correctly per release notes knowledge. No live fetch. |
| "`rebase --update-refs` carries a whole stack of dependent branches" — floor 2.38 | ch05 Synchronization; backmatter floors | [23] git-rebase(1) | yes — `--update-refs` introduced in 2.38, semantics as described. No live fetch. |
| "`git maintenance` 2.30 · `bisect --first-parent` 2.29" | backmatter Feature floors | [32] git-maintenance(1), [17] git-bisect(1) | partly — `git maintenance` was introduced in 2.29 (not 2.30) per release history; `bisect --first-parent` floor 2.29 is correct. Manuscript off by one for maintenance. No live fetch; flagged for correction. |
| "`receive.denyNonFastForwards` / protected branches" as server-side guard | ch06 The covenant | [29] git-push(1), [30] githooks(5) | yes — config key and pre-receive/update hook enforcement as described. No live fetch. |

Limitation note: Per CRITICAL EXECUTION CONSTRAINT, no bash/web-fetch/file access was used. All source checks above are via trained knowledge of the git documentation corpus, not live resolution of the https://git-scm.com URLs listed in backmatter. Therefore the sample is NOT independently verified against the cited URLs' current contents; the operator must rerun this seat with fetch enabled to complete Pass-2 source resolution formally. No claim above should be treated as gate-verified until fetched.

## Scores (1–5)
accuracy: 4 · clarity: 5 · completeness-for-tier: 4 · density: 5 · originality: 5

## Pass-3 only: findings ledger
| Finding # (from Pass 2) | Status: resolved / rebutted-accepted / still-open | Note |
|---|---|---|
| — | — | Pass 2 review; no Pass-3 ledger yet. |
